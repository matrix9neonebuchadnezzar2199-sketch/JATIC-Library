# 現行コードと指示書の差分

**2026-05-20 更新: #01〜#21 実装完了。** 進捗・残作業は [DEV_STATUS.md](../DEV_STATUS.md) を正本とする。

## 完了サマリ（#21 まで）

| 領域 | 内容 |
|------|------|
| コア DL | 並列 DL、manifest、404 時 1 回再スクレイプ |
| GUI | 4 タブ（保管庫・設定・カレンダー・比較）、進捗ダイアログ、トレイ |
| 保管庫 | ソート、再 DL、削除、タグ、月次エクスポート |
| その他 | Git commit、Run キー、PyInstaller スクリプト |

## 意図的な差分・制限

| 項目 | 採用 |
|------|------|
| Git | 自動 commit のみ。`push` は手動 |
| 再スキャン後 | 地域一覧は再起動で反映（即時リロードなし） |
| 配布 | ソース／uv 開発運用は可。exe はビルド検証前（[DEV_STATUS §5](../DEV_STATUS.md#5-残作業リリース配布まで)） |
| 比較グラフ | 先頭数値列の簡易プロット（本格分析 UI ではない） |

## 次の作業

リリース向け残作業は [DEV_STATUS.md §5](../DEV_STATUS.md#5-残作業リリース配布まで)（R1: PyInstaller 検証から）。
