# 現行コードと指示書 #01〜#11 の差分

**2026-05-20 更新: #01〜#11 照合完了。** 進捗は [DEV_STATUS.md](../DEV_STATUS.md) に集約。#12 以降は指示書未作成（バックログのみ）。

## 完了サマリ（#11）

| 項目 | 内容 |
|------|------|
| ツリー | 年 → 月 (`YYYY_M`) → 地域 ZIP |
| データ源 | ディスク走査 + `_manifest.json` + SQLite マージ |
| 詳細ペイン | パス・サイズ・ハッシュ・URL 等 |
| CSV プレビュー | ZIP 内先頭 CSV を最大 1000 行（polars） |
| 検索 | 地域名インクリメンタルフィルタ |
| コンテキスト | エクスプローラーで開く / パスコピー |

## 意図的な差分

| 項目 | 採用 |
|------|------|
| ソート UI | 走査順は日付降順固定（設定の `library_default_sort` は #12 以降） |
| 再 DL / 削除 | 未実装（#13） |
| 欠損月バッジ | 未実装（#14） |

## 次の作業

指示書 #12 の番号割当前に、[DEV_STATUS.md §5](../DEV_STATUS.md) のバックログで優先度を確定する。
