# 現行コードと指示書 #01〜#11 の差分

**2026-05-20 更新: #01〜#11 照合完了。** 以降は #12 以降（進捗 UI・トレイ等）。

## 完了サマリ（#11）

| 項目 | 内容 |
|------|------|
| ツリー | 年 → 月 (`YYYY_M`) → 地域 ZIP |
| データ源 | ディスク走査 + `_manifest.json` + SQLite マージ |
| 詳細ペイン | パス・サイズ・ハッシュ・URL 等 |
| CSV プレビュー | ZIP 内先頭 CSV を最大 1000 行（polars） |
| 検索 | 地域名インクリメンタルフィルタ |
| コンテキスト | エクスプローラーで開く / パスコピー |

## 意図的な差分

| 項目 | 採用 |
|------|------|
| ソート UI | 走査順は日付降順固定（設定の `library_default_sort` は #12 以降） |
| 再 DL / 削除 | 未実装（#13） |
| 欠損月バッジ | 未実装（#14） |

## 次の指示書

#12 進捗ダイアログ / ダウンロード UI 連携、または #13 右クリック拡張。
